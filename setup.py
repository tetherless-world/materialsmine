#!/usr/bin/env python

from distutils.core import setup

setup(name='Nanomine',
      version='0.1',
      description='Nanomine project configuration file',
      author='rui',
      packages=[
              'pymongo'
          ],
     )
